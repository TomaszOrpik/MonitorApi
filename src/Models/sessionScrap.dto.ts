export interface SessionScrap {
    windowWidth: number,
    windowHeigth: number,
    currentPage: string,
    scrollTopPosition: number,
    mouseX: number,
    mouseY: number,
    clickedItemId: string
}